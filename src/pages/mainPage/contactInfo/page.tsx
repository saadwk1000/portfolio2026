import classNames from "classnames";
import styles from "./style.module.scss";

const ContactInfo = () => {
  return (
    <div className={classNames(styles.mainContainer)}>
      <h1>Hello Contact Info</h1>
    </div>
  );
};

export default ContactInfo;
