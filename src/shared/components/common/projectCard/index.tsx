import classNames from "classnames";
import styles from "./style.module.scss";

const ProjectCard = () => {
  return (
    <div className={classNames(styles.mainContainer)}>
      <h1>Hello Project Card</h1>
    </div>
  );
};

export default ProjectCard;
