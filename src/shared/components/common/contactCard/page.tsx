import classNames from "classnames";
import styles from "./style.module.scss";

const ContactCard = () => {
  return (
    <div className={classNames(styles.mainContainer)}>
      <h1>Hello Contact Card</h1>
    </div>
  );
};

export default ContactCard;
